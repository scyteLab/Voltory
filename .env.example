# Copy this file to .env and fill in real values from your Supabase
# project (Project Settings → API). .env is gitignored — never commit it.

VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Only needed locally to run scripts/seed-supabase.mjs — this key
# bypasses Row Level Security, so it must never ship to the browser
# or get committed. Project Settings → API → service_role secret.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ---- Paystack (Session 35a) ----
# Public key from https://dashboard.paystack.com/#/settings/developers
# Use pk_test_... during development, pk_live_... in production.
# Feature is silently off if this is unset; card/transfer/ussd payments
# fall back to the "unpaid" state (like Pay on Delivery).
VITE_PAYSTACK_PUBLIC_KEY=pk_test_your_public_key

# ---- Resend / Email (Session 35b) ----
# Browser-safe: admin address to CC on new orders (optional).
# If unset, no admin notification is sent (customer confirmation still fires).
VITE_ADMIN_NOTIFY_EMAIL=admin@voltory.ng

# NETLIFY FUNCTION env vars (set these in the Netlify dashboard,
# NOT in your local .env — they never touch the browser):
#
#   RESEND_API_KEY       = re_xxxxxxxxxxxx (from resend.com/api-keys)
#   RESEND_FROM_EMAIL    = "Voltory <orders@voltory.ng>"  (verified sender)
#   RESEND_ADMIN_EMAIL   = admin@voltory.ng (fallback recipient for admin notifies)
#
# The send-email Netlify function reads these at runtime.
